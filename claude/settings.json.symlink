{
  "sandbox": {
    "enabled": true,
    "filesystem": {
      "deniedPaths": ["~/.ssh", "~/.aws", "~/.gnupg", "~/.config"]
    },
    "network": {
      "allowedDomains": [],
      "mode": "restricted"
    }
  }
}